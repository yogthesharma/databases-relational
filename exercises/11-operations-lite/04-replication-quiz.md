# 04 — Replication quiz

True / false (write T or F):

1. ___ A replica is a complete substitute for backups.
2. ___ Async replicas can return slightly stale reads.
3. ___ Read replicas can offload reporting queries from the primary.
4. ___ A bad `DELETE` on the primary will not appear on replicas that replay WAL.
