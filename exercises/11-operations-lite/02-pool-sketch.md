# 02 — Pool sketch

Answer in a sentence each:

1. Why does a Node `pg.Pool` help under many concurrent HTTP requests?
2. When would you add **PgBouncer** (or a cloud pooler) in front of Postgres?
3. Roughly: if 20 app instances each open a pool of `max: 20`, what can go wrong vs `max_connections`?
