# 05 — Testing DB code

Describe a 3-step test for the transfer use case:

1. Arrange (seed / reset)  
2. Act (call transfer)  
3. Assert (SQL check of balances)  

Bonus: how would a transactional test (`BEGIN` … `ROLLBACK`) help?
