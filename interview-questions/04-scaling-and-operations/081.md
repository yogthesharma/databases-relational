# Question 081: Observability: pg_stat_statements and Wait Events

## Context
p99 latency rose but CPU is moderate. You have `pg_stat_statements`, `pg_stat_activity`, and wait event metrics from the host.

## Interviewer Prompt
1. How do you attribute latency to specific queries versus waits (locks, IO, LWLock)?
2. What sampling pitfalls mislead you when interpreting `pg_stat_statements`?
3. Outline a triage playbook from "API slow" to root cause in the database.
