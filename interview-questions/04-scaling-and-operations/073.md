# Question 073: Zero-Downtime Expand/Contract Migrations

## Context
You must rename `users.name` to `users.display_name` and backfill 50M rows without downtime. App servers roll out gradually (blue/green).

## Interviewer Prompt
1. Walk through an expand → backfill → dual-write/read → contract sequence.
2. How do you backfill without long locks or replication lag spikes?
3. What mistakes in ORM "auto migrations" commonly cause outages here?
